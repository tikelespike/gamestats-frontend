// Local configuration for the frontend. This will not be included in the final image and has to be
// generated during deployment depending on the environment.

window.__APP_CONFIG__ = {
  // The URL of the backend API.
  apiBaseUrl: "http://localhost:8080/api/v1",
}
